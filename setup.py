from setuptools import setup, find_packages

setup(
    name="wizards-of-worderly-place",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "pytest",
        "termcolor",
    ],
) 